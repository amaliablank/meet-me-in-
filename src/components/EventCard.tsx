import React from 'react'
export default function EventCard({e}:{e:any}){ return (<div className="event-card"><div style={{display:'flex',justifyContent:'space-between'}}><strong>{e.event_name || 'Unknown'}</strong><span style={{fontSize:12}}>{e.start_time? new Date(e.start_time).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'}) : 'TBA'}</span></div><div style={{fontSize:13,color:'var(--muted)'}}>{e.genre || 'Various'} • {e.ticket_price || 'TBA'}</div><a href={e.source_link} target="_blank" rel="noreferrer" style={{fontSize:12,color:'var(--accent)'}}>Source</a></div>) }
